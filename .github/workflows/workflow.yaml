name: CI Test Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "🔍 Run quick sanity check 🔍"
        run: |
          echo "Running CI test workflow"
          echo "Current directory: $(pwd)"
          echo "Files:"
          ls -la
          echo "OK"
