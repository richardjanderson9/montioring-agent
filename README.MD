# Monitoring Agent

This repository contains a minimal test setup for GitHub Actions.

- Workflow: `.github/workflows/workflow.yaml` — runs a simple sanity check on pushes and pull requests to `main`.

To test locally, push a change to the `main` branch or open a PR targeting `main`.
